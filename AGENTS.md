## SQL Optimizer

当我们让 claude code 写 sql 语句的时候，往往他给出的sql语句能达到这样的效果：

**能建好表、插入好数据，也能查准数据。**

在本地一跑，发现很完美。但是这样的sql语句存在一些问题：

只是一个Demo,能查清楚不代表上线后够稳定，在大规模生产上，这样的sql语句会存在极大的性能开销。

好在我们有很多 ** 套路 **，可以快速解决很多常见的sql慢查询、占用内存大等问题。

本skill在建立数据库约束、增删改查业务逻辑上，从标准生产级代码规范、高性能技术栈和SQL优化思路上，帮助后端程序员快速实现高性能的sql语句。